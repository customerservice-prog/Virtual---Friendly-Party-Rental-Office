{
  "name": "friendly-party-rental-office",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A supervised, observable virtual showroom for Friendly Party Rental.",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "build": "node scripts/build-showroom.mjs",
    "prestart": "npm run build",
    "start": "node --env-file-if-exists=.env server.mjs",
    "predev": "npm run build",
    "dev": "node --watch --env-file-if-exists=.env server.mjs",
    "pretest": "npm run build",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && node --check bootstrap.mjs && node --check hosted-check.mjs && node --check scripts/verify-showroom.mjs && node --check server.mjs && node --check lib/store.mjs && node --check lib/engine.mjs && node --check lib/integrations.mjs && node --check public/app.js && node --check public/scene.js"
  }
}
