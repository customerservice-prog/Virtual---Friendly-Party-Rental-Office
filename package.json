{
  "name": "friendly-party-rental-office",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "A supervised, observable 3D virtual office for Friendly Party Rental.",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --watch --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check bootstrap.mjs && node --check hosted-check.mjs && node --check server.mjs && node --check lib/store.mjs && node --check lib/engine.mjs && node --check lib/integrations.mjs && node --check public/app.js && node --check public/scene.js"
  }
}
